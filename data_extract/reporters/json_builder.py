def build_json_report(results):
    """
    Format the audited records into a final JSON-friendly structure.
    Currently, the auditor already outputs a comprehensive structure,
    so we simply pass it through.
    """
    return results
